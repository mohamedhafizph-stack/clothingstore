<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>About Us | Wearify Dark</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    
    <style>
        :root {
            --bg-black: #0a0a0a;
            --card-gray: #161616;
            --border-gray: #2a2a2a;
            --accent-blue: #007aff; /* Electric iOS Blue */
            --text-main: #ffffff;
            --text-muted: #a0a0a0;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', sans-serif;
        }

        body {
            background-color: var(--bg-black);
            color: var(--text-main);
            line-height: 1.6;
            overflow-x: hidden;
        }

        /* --- Navigation --- */
        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 8%;
            background: rgba(10, 10, 10, 0.8);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid var(--border-gray);
            position: sticky;
            top: 0;
            z-index: 1000;
        }

        .logo {
            font-size: 1.6rem;
            font-weight: 700;
            text-decoration: none;
            color: var(--text-main);
            letter-spacing: -1px;
        }

        .logo span { color: var(--accent-blue); }

        .nav-links {
            list-style: none;
            display: flex;
            gap: 30px;
        }

        .nav-links a {
            text-decoration: none;
            color: var(--text-muted);
            font-weight: 500;
            transition: 0.3s;
        }

        .nav-links a:hover { color: var(--accent-blue); }

        /* --- Hero Section --- */
        .hero {
            position: relative;
            height: 70vh;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            background: radial-gradient(circle at center, #1a1a1a 0%, #0a0a0a 100%);
            padding: 0 20px;
        }

        .hero-content h1 {
            font-size: clamp(2.5rem, 8vw, 4.5rem);
            font-weight: 800;
            margin-bottom: 20px;
            background: linear-gradient(to bottom, #fff 30%, #666 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .hero-content p {
            font-size: 1.25rem;
            color: var(--text-muted);
            max-width: 600px;
            margin: 0 auto;
        }

        /* --- Story Section --- */
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 100px 20px;
        }

        .grid-2 {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 80px;
            align-items: center;
        }

        .story-content h2 {
            font-size: 2.5rem;
            margin-bottom: 25px;
        }

        .story-content p {
            color: var(--text-muted);
            margin-bottom: 20px;
            font-size: 1.1rem;
        }

        .story-image {
            position: relative;
        }

        .story-image img {
            width: 100%;
            border-radius: 12px;
            filter: grayscale(20%);
            border: 1px solid var(--border-gray);
        }

        .story-image::after {
            content: '';
            position: absolute;
            top: -15px;
            right: -15px;
            width: 100%;
            height: 100%;
            border: 2px solid var(--accent-blue);
            border-radius: 12px;
            z-index: -1;
        }

        /* --- Values Section --- */
        .values-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 25px;
            margin-top: 60px;
        }

        .value-card {
            background: var(--card-gray);
            padding: 50px 30px;
            border-radius: 16px;
            border: 1px solid var(--border-gray);
            transition: 0.3s;
        }

        .value-card:hover {
            border-color: var(--accent-blue);
            background: #1c1c1c;
            transform: translateY(-5px);
        }

        .value-card i {
            font-size: 2.5rem;
            color: var(--accent-blue);
            margin-bottom: 20px;
        }

        .value-card h3 {
            margin-bottom: 15px;
            font-size: 1.4rem;
        }

        .value-card p {
            color: var(--text-muted);
        }

        /* --- Footer --- */
        footer {
            border-top: 1px solid var(--border-gray);
            padding: 80px 8% 40px;
            background: var(--bg-black);
        }

        .footer-grid {
            display: grid;
            grid-template-columns: 2fr 1fr 1fr 1fr;
            gap: 50px;
            margin-bottom: 60px;
        }

        .footer-col h4 {
            color: var(--text-main);
            margin-bottom: 25px;
            text-transform: uppercase;
            font-size: 0.8rem;
            letter-spacing: 2px;
        }

        .footer-col ul { list-style: none; }
        .footer-col ul li { margin-bottom: 12px; }
        .footer-col ul li a {
            color: var(--text-muted);
            text-decoration: none;
            transition: 0.2s;
        }
        .footer-col ul li a:hover { color: var(--text-main); }

        .social-links {
            display: flex;
            gap: 20px;
            margin-top: 20px;
        }

        .social-links a {
            color: var(--text-muted);
            font-size: 1.5rem;
            transition: 0.3s;
        }

        .social-links a:hover { color: var(--accent-blue); }

        .footer-bottom {
            text-align: center;
            color: #444;
            font-size: 0.85rem;
            padding-top: 40px;
            border-top: 1px solid var(--border-gray);
        }

        /* --- Responsive --- */
        @media (max-width: 968px) {
            .footer-grid { grid-template-columns: 1fr 1fr; }
            .grid-2 { grid-template-columns: 1fr; gap: 50px; }
            .hero-content h1 { font-size: 3rem; }
        }
    </style>
</head>
<body>

    <nav>
        <a href="/" class="logo">WEARI<span>FY</span></a>
        <ul class="nav-links">
            <li><a href="/home">Collection</a></li>
            <li><a href="/home">Shop</a></li>
            <li><a href="/about-us">About</a></li>
        </ul>
    </nav>

    <section class="hero">
        <div class="hero-content">
            <h1>Elegance in Every Stitch.</h1>
            <p>We combine minimalist aesthetics with premium craftsmanship to create the future of your wardrobe.</p>
        </div>
    </section>

    <section class="container">
        <div class="grid-2">
            <div class="story-content">
                <h2 style="color: var(--accent-blue);">The Narrative.</h2>
                <p>Wearify was born in 2024 out of a desire to simplify the complex world of fashion. We believe that true style isn't loud—it's felt. </p>
                <p>By cutting out the middlemen and focusing on sustainable, long-staple cotton and recycled tech-fabrics, we deliver luxury-grade apparel directly to your door.</p>
                <p>Every piece is a testament to our obsession with detail, from the reinforced seams to our custom-milled hardware.</p>
            </div>
            <div class="story-image">
                <img src="https://images.unsplash.com/photo-1550639525-c97d455acf70?ixlib=rb-1.2.1&auto=format&fit=crop&w=800&q=80" alt="Dark Fashion Aesthetic">
            </div>
        </div>
    </section>

    <section style="background: #0f0f0f; border-top: 1px solid var(--border-gray); border-bottom: 1px solid var(--border-gray);">
        <div class="container">
            <div style="text-align: center; margin-bottom: 20px;">
                <h2 style="font-size: 2rem;">Built Differently.</h2>
            </div>
            <div class="values-grid">
                <div class="value-card">
                    <i class="fas fa-microchip"></i>
                    <h3>Tech-Driven</h3>
                    <p>We use smart-fit technology to ensure our sizing is consistent across every single category.</p>
                </div>
                <div class="value-card">
                    <i class="fas fa-bolt"></i>
                    <h3>Conscious Speed</h3>
                    <p>While we ship fast, we produce slowly. We believe in small-batch drops to prevent textile waste.</p>
                </div>
                <div class="value-card">
                    <i class="fas fa-shield-alt"></i>
                    <h3>Ironclad Quality</h3>
                    <p>If your garment doesn't last at least 100 washes, we'll replace it. That's the Wearify Guarantee.</p>
                </div>
            </div>
        </div>
    </section>

    <footer>
        <div class="footer-grid">
            <div class="footer-col">
                <a href="/" class="logo">WEARI<span>FY</span></a>
                <p style="color: var(--text-muted); margin-top: 20px; font-size: 0.9rem;">Elevating everyday essentials through modern design and ethical production.</p>
                <div class="social-links">
                    <a href="#"><i class="fab fa-instagram"></i></a>
                    <a href="#"><i class="fab fa-twitter"></i></a>
                    <a href="#"><i class="fab fa-discord"></i></a>
                </div>
            </div>
            <div class="footer-col">
                <h4>Experience</h4>
                <ul>
                    <li><a href="/shop">New Arrivals</a></li>
                    <li><a href="/shop">Best Sellers</a></li>
                    <li><a href="/about">Our Story</a></li>
                </ul>
            </div>
            <div class="footer-col">
                <h4>Guidelines</h4>
                <ul>
                    <li><a href="#">Sustainability</a></li>
                    <li><a href="#">Size Guide</a></li>
                    <li><a href="#">Shipping</a></li>
                </ul>
            </div>
            <div class="footer-col">
                <h4>Contact</h4>
                <ul>
                    <li><a href="mailto:hello@wearify.com">hello@wearify.com</a></li>
                    <li><a href="#">Support Center</a></li>
                </ul>
            </div>
        </div>
        <div class="footer-bottom">
            &copy; 2026 WEARIFY CO. PROJECTED IN DARK MODE.
        </div>
    </footer>

</body>
</html>